<!DOCTYPE html>
<html>
  <head>
    <title>Poker App</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Poker App</h1>
    <p>Welcome to poker app</p>
    <div>
      <form action="/judge" method="post">
        <label for="cards">カードを入力してください: </label>
        <input type="text" name="card_field" placeholder='A1 A2 A3 A4 A5' value=<%= placeholder %>>
        <input type="submit" value="OK"/>
      </form>
      <!-- <% if (typeof params == 'undefined') { %>
        <p>カードを入力してください</p>
      <% } else { %>
        <p><%= params %></p>
      <% } %> -->
    </div>
    <div>
      <form action="/get_card" method="get">
      
      </form>
      <% if (typeof hands == 'undefined') { %>
        <p></p>
      <% } else { %>
        <p>役名: <%= hands.name %></p>
        <p>スコア: <%= hands.score %></p>
      <% } %>
    </div>
    <div>
      <form action="/" method="get">
        <button type="submit" value="最初から">最初から</button>
      </form>
    </div>
  </body>
</html>
